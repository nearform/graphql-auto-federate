'use strict'

const kType = Symbol('type')
const kName = Symbol('name')
const kValues = Symbol('values')
const kFields = Symbol('fields')
const kTypes = Symbol('types')
const kArgs = Symbol('args')
const kDirectives = Symbol('directives')
const kDefault = Symbol('default')

module.exports = {
  kType,
  kName,
  kValues,
  kTypes,
  kFields,
  kArgs,
  kDirectives,
  kDefault
}
